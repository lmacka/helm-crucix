apiVersion: v1
kind: Service
metadata:
  name: {{ include "crucix.fullname" . }}
  labels:
    {{- include "crucix.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "crucix.selectorLabels" . | nindent 4 }}
