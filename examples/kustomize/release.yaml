---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: crucix
spec:
  interval: 1h
  chart:
    spec:
      chart: crucix
      version: 0.1.0
      sourceRef:
        kind: HelmRepository
        name: crucix
  values:
    image:
      tag: latest
      pullPolicy: Always
